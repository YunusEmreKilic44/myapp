import 'package:flutter/material.dart';

class Takvim extends StatelessWidget {
  const Takvim({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text("Akademik Takvim")));
  }
}
